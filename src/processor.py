def filter_new_articles(articles):
    """
    Filtre les articles pour ne garder que les nouveautés.
    (Version de base pour tester le pipeline)
    """
    # Pour l'instant, on retourne tous les articles récupérés
    return articles
